# MongoDB (required for seed, weekly sync, DB updates)
MONGODB_URI=mongodb://localhost:27017/lawagents

# OpenAI (required for crawl-datarade-legal-apis, semantic scoring)
OPENAI_API_KEY=sk-...

# Optional: Reddit (for sync-tools community insights)
REDDIT_CLIENT_ID=...
REDDIT_CLIENT_SECRET=...

# Optional: X/Twitter
X_BEARER_TOKEN=...

# Optional: Threads
THREADS_ACCESS_TOKEN=...
